# =========================
# Configuration for WhatsAsena bot
# =========================

# --- Session / Pairing ---
# ASENA_SESSION : base64 d'un JSON de credentials (généré par make_string_session_no_qr.js
# ou encodé automatiquement après un pairing). Laisser vide pour faire un pairing manuel (QR ou PAIR_NUMBER).
# Exemple (NE PAS COLLER tel quel) : ASENA_SESSION=eyJjcmlkIjogeyJfX3R5cGVfXyI6eyJkYXRhIjoi... (long)
ASENA_SESSION=

# PAIR_NUMBER : si tu veux appairer par numéro (countrycode + number, sans + ni espaces)
# Exemple pour Cameroun : 237676990829
# Laisser vide pour utiliser un QR.
PAIR_NUMBER=

# --- Runtime / env ---
NODE_ENV=production

# DEBUG : true ou false — active logs plus verbeux si true
DEBUG=false

# CLR_SESSION : true pour forcer la suppression du dossier ./auth au démarrage (utile pour "reset" de session)
CLR_SESSION=false

# HANDLERS : regex des préfixes de commandes (optionnel) — la valeur par défaut dans config.js est "^[.]"
# HANDLERS=^[.]   # exemple : "." comme préfix
HANDLERS=^[.]

# LANGUAGE : code langue (EN, FR, ...)
LANGUAGE=EN

# --- Database ---
# DATABASE_URL : par défaut le projet utilise sqlite ./whatsasena.db si laissé vide.
# Pour Postgres ou autre, mettre l'URL complète :
# postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=./whatsasena.db

# --- Optional / Misc (laisser vides si non utilisés) ---
# SEND_READ : true/false -> si le bot doit marquer les messages comme lus
SEND_READ=false

# BRANCH : branche de code (utilisé parfois pour mises à jour automatisées)
BRANCH=master

# SUDO : identifiants ou JIDs administrateurs (optionnel)
# SUDO=123456789@s.whatsapp.net

# HEROKU settings (si tu déployes sur Heroku)
HEROKU=false
HEROKU_API_KEY=
HEROKU_APP_NAME=

# Autres clés/flags du projet (laisser si tu n'en as pas besoin)
REMOVEBG_KEY=
WARN_COUNT=3
WARN_MSG=Ok bie

# =========================
# Notes d'utilisation
# =========================
# 1) Pour générer ASENA_SESSION proprement (recommandé) : exécute ton script make_string_session_no_qr.js
#    qui peut produire la string base64 à coller ici.
# 2) Si tu mets ASENA_SESSION, le bot va tenter de restaurer la session depuis cette valeur
#    (plus besoin de pairing). La string doit être base64 d'un JSON correctement sérialisé.
# 3) Si tu veux appairer par numéro, remplis PAIR_NUMBER (ex: 237676990829). Laisser PAIR_NUMBER vide déclenche le QR.
# 4) Ajoute config.env à .gitignore :
#    echo "config.env" >> .gitignore
# 5) Après modification, démarre le bot :
#    npm start
#
# Besoin d'aide pour remplir une valeur particulière (ex: DATABASE_URL ou ASENA_SESSION) ?
# Dis-moi laquelle et je t'aide pas à pas t.me/hinugera.
